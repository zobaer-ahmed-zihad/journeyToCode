#include<stdio.h>
#include<math.h>
#include<stdlib.h>
int main(){
    //ceil
    // float a;
    // scanf("%f", &a);
    // int result = ceil(a);

    //floor
    // float a;
    // scanf("%f", &a);
    // int result = floor(a);

    //Round
    // float a;
    // scanf("%f", &a);
    // int result = round(a);

    //sqrt
    // float a;
    // scanf("%f", &a);
    // int result = sqrt(a);

    //power
    // float a, b;
    // scanf("%f %f", &a, &b);
    // float result = pow(a , b);

    //absolute 
    int a;
    scanf("%d", &a);
    int result = abs(a);
    printf("%d", result);
    

    return 0;
}