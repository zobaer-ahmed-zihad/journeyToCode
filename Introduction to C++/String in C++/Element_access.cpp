// S[i] -> access the ith index of the string.
// s.at(i) -> access the ith index of the string.
// s.back() -> access the last element of the string.
// s.front() -> access the first element of the string.
#include<bits/stdc++.h>
using namespace std;
int main(){
   string st = "Hello I am zihad";
    // cout<<st[4]<<endl;
    // cout<<st.at(6)<<endl;
    cout<<st.back()<<endl;
    cout<<st.front();

   return 0;
}